import { createStyles, makeStyles } from '@mui/styles';

const useStyles = makeStyles(() =>
  createStyles({
    textField: {
      width: '100%',
      marginBottom: '1.5rem',
    },
  })
);

export default useStyles;
