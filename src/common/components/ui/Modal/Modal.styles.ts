import { createStyles, makeStyles } from '@mui/styles';

const useStyles = makeStyles(() =>
  createStyles({
    container: {
      padding: '2rem',
      paddingBottom: '2rem',
    },
  })
);

export default useStyles;
